import { IApplicationInternal } from '../application-internal.interface';
/**
 * Mount the application to the DOM
 */
export declare const mount: <TProps>(this: IApplicationInternal<TProps>) => void;
