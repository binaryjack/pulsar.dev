import { IApplicationInternal } from '../application-internal.interface';
/**
 * Unmount the application from the DOM
 */
export declare const unmount: <TProps>(this: IApplicationInternal<TProps>) => void;
