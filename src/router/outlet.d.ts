/**
 * Outlet component
 * Renders nested routes
 */
/**
 * Outlet component for nested routes
 * Renders the matched child route
 *
 * @example
 * <Route path="/dashboard">
 *   <Outlet />
 * </Route>
 */
export declare const Outlet: () => HTMLElement;
