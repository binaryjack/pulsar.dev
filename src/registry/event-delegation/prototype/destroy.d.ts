/**
 * Destroy delegator and cleanup all listeners
 */
import type { IRegistryEventDelegator } from '../registry-event-delegator.types';
/**
 * Destroy delegator and cleanup all listeners
 */
export declare const destroy: (this: IRegistryEventDelegator) => void;
