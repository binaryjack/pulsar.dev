/**
 * Unregister all handlers for an element
 */
import type { IRegistryEventDelegator } from '../registry-event-delegator.types';
/**
 * Unregister all handlers for an element
 */
export declare const unregisterElement: (this: IRegistryEventDelegator, elementId: string) => void;
