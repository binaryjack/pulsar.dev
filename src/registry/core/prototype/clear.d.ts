/**
 * Clear method for ElementRegistry
 * Clears all entries from the registry
 */
import type { IElementRegistry } from '../element-registry.types';
export declare function clear(this: IElementRegistry): void;
