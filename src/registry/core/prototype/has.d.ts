/**
 * Has method for ElementRegistry
 * Checks if an element is registered
 */
import type { IElementRegistry } from '../element-registry.types';
export declare function has(this: IElementRegistry, id: string): boolean;
