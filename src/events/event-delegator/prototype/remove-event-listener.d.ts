import { IEventDelegator } from '../event-delegator.types';
/**
 * Removes an event listener from an element
 */
export declare const removeEventListener: (this: IEventDelegator, element: HTMLElement, eventType: string) => void;
