/**
 * Resource Clear Method
 *
 * Resets resource to idle state, clearing data and errors.
 */
import { IResourceInternal } from '../resource.types';
export declare const clear: <T>(this: IResourceInternal<T>) => void;
